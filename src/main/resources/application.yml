api:
  url:
    contactUs: http://google.com

  contactUs:
    portalname: ISPF


spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ispf
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
  properties:
    hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect
  mvc:
    view:
      prefix: /WEB-INF/pages/
      suffix: .jsp


server:
  servlet:
    context-path: /